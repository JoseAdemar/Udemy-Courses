var minhaVar = "Minha variável";

function minhaFunc(x,y){
    return x + y;
}

let num = 2;
const PI = 3.14;

var numeros = [1,2,3];
numeros.map(function(valores){
  return valores * 2;
});

numeros.map(valor => valor * 2);